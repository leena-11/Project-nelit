services:
  - type: web
    name: smart-restaurant
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate
    startCommand: gunicorn smart_restaurant.wsgi:application
